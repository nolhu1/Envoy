# Envoy Web App

This package contains Envoy's main product UI and active API routes.

For local setup, quickstart, and workspace-level commands, use the root project README:

- [Root README](C:/Users/admin/Envoy/README.md)

Common local commands from the repo root:

```bash
pnpm dev:web
pnpm lint
pnpm build
```
